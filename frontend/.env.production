# Production environment - override with actual deployment values
# These can be set via Docker build args or environment variables at runtime
VITE_AUTH_AUTHORITY=https://keycloak.olympus.prod/realms/olympus
VITE_AUTH_REDIRECT_URI=https://app.olympus.prod
VITE_API_URL=https://api.olympus.prod/api/v1
